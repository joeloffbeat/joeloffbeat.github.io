{
  "name": "joeloffbeat-mindscape",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "vite",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "prefetch": "node scripts/prefetch-spotify.js",
    "prebuild": "node scripts/prefetch-spotify.js",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "marked": "^17.0.4",
    "three": "^0.182.0"
  },
  "devDependencies": {
    "dotenv": "^17.3.1",
    "gh-pages": "^6.3.0",
    "vite": "^7.3.0"
  }
}
