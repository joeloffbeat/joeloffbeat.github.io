{
  "name": "joeloffbeat-mindscape",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "vite",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "three": "^0.182.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^7.3.0"
  }
}
